.SILENT:

VMFILES = hbonds.tcl pkgIndex.tcl
VMVERSION = 1.2
DIR = $(PLUGINDIR)/noarch/tcl/hbonds$(VMVERSION)

bins:
win32bins:
dynlibs:
staticlibs:
win32staticlibs:

distrib:
	@echo "Copying hbonds $(VMVERSION) files to $(DIR)"
	mkdir -p $(DIR) 
	cp $(VMFILES) $(DIR) 

	

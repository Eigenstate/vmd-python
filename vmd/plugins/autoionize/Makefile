.SILENT:

VMFILES = autoionize.tcl autoionizegui.tcl pkgIndex.tcl
VMVERSION = 1.5
DIR = $(PLUGINDIR)/noarch/tcl/autoionize$(VMVERSION)

bins:
win32bins:
dynlibs:
staticlibs:
win32staticlibs:

distrib:
	@echo "Copying autoionize $(VMVERSION) files to $(DIR)"
	mkdir -p $(DIR) 
	cp $(VMFILES) $(DIR) 

	

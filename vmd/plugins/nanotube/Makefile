.SILENT:

VMFILES = gui.tcl graphene.tcl nanotube.tcl pkgIndex.tcl
VMVERSION = 1.6
DIR = $(PLUGINDIR)/noarch/tcl/nanotube$(VMVERSION)

bins:
win32bins:
dynlibs:
staticlibs:
win32staticlibs:

distrib:
	@echo "Copying nanotube $(VMVERSION) files to $(DIR)"
	mkdir -p $(DIR) 
	cp $(VMFILES) $(DIR) 

	

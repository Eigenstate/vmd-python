.SILENT:

VMFILES = parseFEP.tcl pkgIndex.tcl
VMVERSION = 2.1
DIR = $(PLUGINDIR)/noarch/tcl/parsefep$(VMVERSION)

bins:
win32bins:
dynlibs:
staticlibs:
win32staticlibs:

distrib:
	@echo "Copying parsefep $(VMVERSION) files to $(DIR)"
	mkdir -p $(DIR) 
	cp $(VMFILES) $(DIR) 

	
